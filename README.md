# PenPal-Robot
Processing code that interprets a .jpg file the user drew and creates a text file with the machine instructions (positions, origin finding, tool/pen selection) that I call the W code (Similar to G code for CNC machines but W for Write). The text file exported from this program can be loaded onto an SD card and loaded to PenPal.

The arduino code is also available in this repository.
